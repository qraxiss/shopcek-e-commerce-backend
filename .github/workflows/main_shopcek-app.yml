# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy Node.js app to Azure Web App - shopcek-app

env: HOST:127.0.0.1
    PORT:8080
    APP_KEYS:cfs2hUWG56MQ3eQyHA4HRA==,XbGg500vq3PZHhRTpLgkkA==,nmRKbgpuLEHPUZzj+g8FQg==,W0FUzGf4qtKjpNPqAT37WQ==
    API_TOKEN_SALT:gpYtl18NjrBdNjZCIyG0IA==
    ADMIN_JWT_SECRET:BE8eX3NEalHnebBOAvJCjg==
    TRANSFER_TOKEN_SALT:zS5yiJGAO+bT3el+wEebHw==
    DATABASE_CLIENT:mysql
    DATABASE_HOST:shopcek-mysql-server.mysql.database.azure.com
    DATABASE_PORT:3306
    DATABASE_NAME:strapi
    DATABASE_USERNAME:qraxiss
    DATABASE_PASSWORD:"1]uEQcM'to;YClY2qFhRQ-bV3h,#KMVITICtddbl,[9U))+qOY"
    DATABASE_SSL:true
    JWT_SECRET:Z5dYQ2lJ1Zov7IdlARXL4A==
    PRINTFUL_TOKEN:OPTL28OgyJZM4yUjlxWPjYWaJdGJOeZjE4VTzwZW
    PRINTFUL_STORE_ID:13279781S
    NODE_ENV:production

on:
    push:
        branches:
            - main
    workflow_dispatch:

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v4

            - name: Set up Node.js version
              uses: actions/setup-node@v3
              with:
                  node-version: '18.x'

            - name: npm install, build, and test
              run: |
                  npm install
                  npm run build

            - name: Zip artifact for deployment
              run: zip release.zip ./* -r

            - name: Upload artifact for deployment job
              uses: actions/upload-artifact@v3
              with:
                  name: node-app
                  path: release.zip

    deploy:
        runs-on: ubuntu-latest
        needs: build
        environment:
            name: 'Production'
            url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}
        permissions:
            id-token: write #This is required for requesting the JWT

        steps:
            - name: Download artifact from build job
              uses: actions/download-artifact@v3
              with:
                  name: node-app

            - name: Unzip artifact for deployment
              run: unzip release.zip

            - name: Login to Azure
              uses: azure/login@v1
              with:
                  client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_982270D822564D5187D3F1AF9CA6A0AD }}
                  tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_6C0F93261F8A45B6877B227BCE71E5AF }}
                  subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_7B03360860584EB18915C0C0522DE72A }}

            - name: 'Deploy to Azure Web App'
              id: deploy-to-webapp
              uses: azure/webapps-deploy@v2
              with:
                  app-name: 'shopcek-app'
                  slot-name: 'Production'
                  package: .
